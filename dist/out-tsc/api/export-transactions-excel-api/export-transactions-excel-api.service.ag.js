var _a, _b, _c, _d;
import { __decorate, __metadata } from "tslib";
import { Injectable, NgZone, Injector } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { Router } from '@angular/router';
import { ApiBase } from '@plano/shared/api/base/api-base';
import { Id } from '@plano/shared/api/base/id';
import { Meta } from '@plano/shared/api/base/meta';
import { ApiAttributeInfo } from '@plano/shared/api/base/api-attribute-info';
import { PApiPrimitiveTypes } from '@plano/shared/api/base/generated-types.ag';
import { ApiListWrapper, SchedulingApiBooking, ApiObjectWrapper, AuthenticatedApiRole } from '@plano/shared/api';
import { ApiErrorService } from '@plano/shared/api/api-error.service';
/**
 * This service enables access to the api "export_transactions_excel".
 * This file is auto generated by de.sage.scheduler.api_generator.ApiGenerator.
 */
// constants
class Consts {
    constructor() {
        this.TRANSACTION_IDS = 1;
        this.SHIFT_MODEL_IDS = 2;
    }
}
let ExportTransactionsExcelApiService = class ExportTransactionsExcelApiService extends ApiBase {
    constructor(h, router, apiE, zone, injector) {
        super(h, router, apiE, zone, injector, 'export_transactions_excel');
        this.consts = new Consts();
        this.dataWrapper = new ExportTransactionsExcelApiRoot(this);
    }
    version() {
        return '0b02ddbb4b4a8325235624409473ab0d,6f1e237e3f3ce845c3fc1b14ac2d93a9';
    }
    get data() {
        return this.dataWrapper;
    }
    getRootWrapper() {
        return this.dataWrapper;
    }
    recreateRootWrapper() {
        this.dataWrapper = new ExportTransactionsExcelApiRoot(this);
    }
};
ExportTransactionsExcelApiService = __decorate([
    Injectable({
        providedIn: 'root',
    }),
    __metadata("design:paramtypes", [typeof (_a = typeof HttpClient !== "undefined" && HttpClient) === "function" ? _a : Object, typeof (_b = typeof Router !== "undefined" && Router) === "function" ? _b : Object, ApiErrorService, typeof (_c = typeof NgZone !== "undefined" && NgZone) === "function" ? _c : Object, typeof (_d = typeof Injector !== "undefined" && Injector) === "function" ? _d : Object])
], ExportTransactionsExcelApiService);
export { ExportTransactionsExcelApiService };
export class ExportTransactionsExcelApiRoot extends ApiObjectWrapper {
    constructor(api, idRaw = null) {
        super(api, ExportTransactionsExcelApiRoot);
        this.api = api;
        this._id = null;
        this.attributeInfoThis = new ApiAttributeInfo({
            apiObjWrapper: this,
            name: '',
            id: 'ROOT',
            show: function () {
                if (!(((this.api.rightsService.requesterIs(AuthenticatedApiRole.CLIENT_OWNER)) || (this.api.schedulingApi.currentlyDetailedLoaded instanceof SchedulingApiBooking &&
                    this.api.rightsService.hasManagerRightsForShiftModel(this.api.schedulingApi.currentlyDetailedLoaded.shiftModelId))))) {
                    return false;
                }
                return true;
            },
            canEdit: function () {
                if (!(((this.api.rightsService.requesterIs(AuthenticatedApiRole.CLIENT_OWNER)) || (this.api.schedulingApi.currentlyDetailedLoaded instanceof SchedulingApiBooking &&
                    this.api.rightsService.hasManagerRightsForShiftModel(this.api.schedulingApi.currentlyDetailedLoaded.shiftModelId))))) {
                    return false;
                }
                return true;
            },
            readMode: function () {
                if (!(((this.api.rightsService.requesterIs(AuthenticatedApiRole.CLIENT_OWNER))))) {
                    return true;
                }
                return false;
            },
        });
        this.transactionIdsWrapper = new ExportTransactionsExcelApiTransactionIds(this.api, false);
        this.shiftModelIdsWrapper = new ExportTransactionsExcelApiShiftModelIds(this.api, false);
        this._updateRawData(Meta.createNewObject(false, idRaw), true);
        // set parent attribute
        this.transactionIdsWrapper.parent = this;
        this.shiftModelIdsWrapper.parent = this;
    }
    get id() {
        return this._id !== null ? this._id : Id.create(Meta.getNewItemId(this.rawData));
    }
    /**
     *  Id list of the transactions to be exported.
     */
    get transactionIds() {
        return this.transactionIdsWrapper;
    }
    set transactionIdsTestSetter(v) {
        this.setterImpl(1, v.rawData, 'transactionIds', () => { this.transactionIdsWrapper = v; });
    }
    /**
     *  Id list of the shift model to be exported.
     */
    get shiftModelIds() {
        return this.shiftModelIdsWrapper;
    }
    set shiftModelIdsTestSetter(v) {
        this.setterImpl(2, v.rawData, 'shiftModelIds', () => { this.shiftModelIdsWrapper = v; });
    }
    _fixIds(_idReplacements) {
        this.transactionIdsWrapper._fixIds(_idReplacements);
        this.shiftModelIdsWrapper._fixIds(_idReplacements);
    }
    _updateRawData(data, generateMissingData) {
        super._updateRawData(data, generateMissingData);
        this.data = data;
        // update id wrapper
        const idRawData = Meta.getId(data);
        this._id = idRawData === null ? null : Id.create(idRawData);
        // create missing/default data
        if (generateMissingData && data) {
            this.fillWithDefaultValues(data, 3);
            if (data[1] === null)
                data[1] = Meta.createNewList();
            if (data[2] === null)
                data[2] = Meta.createNewList();
        }
        // propagate new raw data to children
        this.transactionIdsWrapper._updateRawData(data ? data[1] : null, generateMissingData);
        this.shiftModelIdsWrapper._updateRawData(data ? data[2] : null, generateMissingData);
    }
    get dni() {
        return '1';
    }
    static loadDetailed(api, id, { success = null, error = null, searchParams = null } = {}) {
        return ApiObjectWrapper.loadDetailedImpl(api, id, '1', { success: success, error: error, searchParams: searchParams });
    }
    assumeValidated() {
        // TODO: PLANO-151346
    }
}
export class ExportTransactionsExcelApiTransactionIds extends ApiListWrapper {
    constructor(api, removeDestroyedItems) {
        super(api, removeDestroyedItems, 'transactionIds');
        this.api = api;
        this.parent = null;
        this.attributeInfoThis = new ApiAttributeInfo({
            apiObjWrapper: this,
            name: 'transactionIds',
            id: 'TRANSACTION_IDS',
            primitiveType: PApiPrimitiveTypes.ApiList,
        });
        this.attributeInfoTransactionId = new ApiAttributeInfo({
            apiObjWrapper: this,
            name: 'transactionId',
            id: 'TRANSACTION_ID',
        });
    }
    wrapItem(item, _generateMissingData) {
        const newWrapper = new ExportTransactionsExcelApiTransactionId(this.api);
        newWrapper._updateRawData(item, _generateMissingData);
        return newWrapper;
    }
    containsPrimitives() {
        return false;
    }
    containsIds() {
        return false;
    }
    createInstance(removeDestroyedItems) {
        return new ExportTransactionsExcelApiTransactionIds(this.api, removeDestroyedItems);
    }
    get dni() {
        return '2';
    }
    createNewItem(id = null) {
        const newItemRaw = Meta.createNewObject(false, !!id ? id.rawData : null);
        const newItem = this.wrapItem(newItemRaw, true);
        this.push(newItem);
        if (this.api)
            this.api.changed('transactionIds');
        return newItem;
    }
}
export class ExportTransactionsExcelApiTransactionId extends ApiObjectWrapper {
    constructor(api, idRaw = null) {
        super(api, ExportTransactionsExcelApiTransactionId);
        this.api = api;
        this.parent = null;
        this._id = null;
        this.attributeInfoThis = new ApiAttributeInfo({
            apiObjWrapper: this,
            name: 'transactionId',
            id: 'TRANSACTION_ID',
        });
        this._updateRawData(Meta.createNewObject(false, idRaw), true);
        // set parent attribute
    }
    get id() {
        return this._id !== null ? this._id : Id.create(Meta.getNewItemId(this.rawData));
    }
    _fixIds(_idReplacements) {
    }
    _updateRawData(data, generateMissingData) {
        super._updateRawData(data, generateMissingData);
        this.data = data;
        // update id wrapper
        const idRawData = Meta.getId(data);
        this._id = idRawData === null ? null : Id.create(idRawData);
        // create missing/default data
        if (generateMissingData && data) {
            this.fillWithDefaultValues(data, 1);
        }
        // propagate new raw data to children
    }
    get dni() {
        return '4';
    }
    static loadDetailed(api, id, { success = null, error = null, searchParams = null } = {}) {
        return ApiObjectWrapper.loadDetailedImpl(api, id, '4', { success: success, error: error, searchParams: searchParams });
    }
    assumeValidated() {
        // TODO: PLANO-151346
    }
}
export class ExportTransactionsExcelApiShiftModelIds extends ApiListWrapper {
    constructor(api, removeDestroyedItems) {
        super(api, removeDestroyedItems, 'shiftModelIds');
        this.api = api;
        this.parent = null;
        this.attributeInfoThis = new ApiAttributeInfo({
            apiObjWrapper: this,
            name: 'shiftModelIds',
            id: 'SHIFT_MODEL_IDS',
            primitiveType: PApiPrimitiveTypes.ApiList,
        });
        this.attributeInfoShiftModelId = new ApiAttributeInfo({
            apiObjWrapper: this,
            name: 'shiftModelId',
            id: 'SHIFT_MODEL_ID',
            primitiveType: PApiPrimitiveTypes.Id,
        });
    }
    wrapItem(item, _generateMissingData) {
        return Id.create(item);
    }
    containsPrimitives() {
        return false;
    }
    containsIds() {
        return true;
    }
    createInstance(removeDestroyedItems) {
        return new ExportTransactionsExcelApiShiftModelIds(this.api, removeDestroyedItems);
    }
    get dni() {
        return '3';
    }
    createNewItem() {
        const newItemRaw = null;
        const newItem = this.wrapItem(newItemRaw, true);
        this.push(newItem);
        if (this.api)
            this.api.changed('shiftModelIds');
        return newItem;
    }
}
//# sourceMappingURL=export-transactions-excel-api.service.ag.js.map